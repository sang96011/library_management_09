<div class="col-md-2 col-md-offset-10">
  <%= button_to t(".button_unfollow"), follow_path(follow: {target_id: @target.id, target_type: @target.class.name}), remote: true, method: :delete,  class: "btn-primary" %>
</div>
