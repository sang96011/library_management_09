<div class="row" id="button">
  <div class="col-md-2 col-md-offset-10">
    <% if current_user.follow?(@book) %>
      <%= render "follows/unfollow" %>
    <% else %>
      <%= render "follows/follow" %>
    <% end %>
  </div>
</div>
